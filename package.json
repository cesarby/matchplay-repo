{
  "name": "matchplay",
  "private": true,
  "scripts": {
    "back": "echo [matchplay-backend] Iniciando Spring Boot... && echo [matchplay-backend] Puerto: http://localhost:8080 && dotenv -e ./backend/.env -- cmd /c \"cd backend && mvnw.cmd spring-boot:run\"",
    "front": "echo [matchplay-frontend] Iniciando Vite... && echo [matchplay-frontend] Puerto: http://localhost:5173 && cd frontend && npm run dev",
    "dev": "npm-run-all --parallel back front"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "npm-run-all": "^4.1.5"
  }
}
